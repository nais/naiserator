testconfig:
  description: applications with an idporten sidecar need the idporten feature toggle
config:
  features:
    idporten: false
input:
  kind: Application
  apiVersion: nais.io/v1alpha1
  metadata:
    name: myapplication
    namespace: mynamespace
  spec:
    idporten:
      sidecar:
        enabled: true
error: "idporten is not available in this cluster"
